**CoverImage** — 2:3 poster tile for game/comic covers. Gradient fallback (shows the alt text) when `src` is missing; optional title scrim, corner badge, and slow breathe loop.

```jsx
<CoverImage src={url} title="Celeste" badge="Platformer" />
<CoverImage alt="Untitled" title="No cover" />
```
