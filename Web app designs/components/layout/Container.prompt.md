**Container** — the page-width wrapper. 1180px max-width, centered, 32px side padding.

```jsx
<Container>
  <p>Page content…</p>
</Container>
```

Use inside Header/Footer/Hero/any full-bleed section so content lines up across the page.
