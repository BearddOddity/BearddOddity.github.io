**NavItem** — sidebar row; active state gets the accent-tinted fill, glow and scaled icon tile.

```jsx
<NavItem icon="📊" label="Dashboard" active />
<NavItem icon="💬" label="Chat" badge="3" />
<NavItem icon="🎛️" label="Sound" collapsed />
```
Set `collapsed` for the 68px icon-only rail.
