**SectionHead** — icon-tile + title + muted description row that heads panels and settings blocks. Optional right-aligned slot for actions.

```jsx
<SectionHead icon="🎛️" title="Sound Board" desc="12 pads · hotkeys enabled"
  right={<Button size="sm">Add pad</Button>} />
```
