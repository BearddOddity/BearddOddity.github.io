**StatusDot** — tiny connection/health indicator; green `on` and amber `warn` glow, grey `off`.

```jsx
<StatusDot status="on" label="Connected" />
<StatusDot status="warn" label="Reconnecting" />
```
