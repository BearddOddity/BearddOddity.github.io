**PricingCard** — plan tile: name, price, feature checklist, CTA. `highlight` marks the recommended tier.

```jsx
<PricingCard name="Pro" price="$12" features={["Unlimited channels","Custom overlays"]}
  highlight action={<Button variant="cta">Start free trial</Button>} />
```
