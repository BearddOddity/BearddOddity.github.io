**Card** — the glassmorphism surface used everywhere: blurred black-alpha fill, hairline border, layered shadow, subtle white top-inset highlight. Border + shadow lift on hover.

```jsx
<Card padding={24}>
  <SectionHead icon="⚙️" title="Engine" desc="Presence scanner status" />
</Card>
```

Wrap content that needs elevation. Same glass fill across every product — no per-theme swap.
