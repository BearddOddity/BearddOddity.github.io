**Alert** — persistent banner for page/section-level notices (unlike `Toast`, which auto-dismisses).

```jsx
<Alert variant="warn" title="Auth expiring soon" onClose={dismiss}>
  Reconnect your Twitch account within 3 days to keep overlays syncing.
</Alert>
```
