**Toast** — transient notification pill; tinted glass, slides in from the right.

```jsx
<Toast variant="success">Overlay connected</Toast>
<Toast variant="error">Auth expired — reconnect</Toast>
```
Variants: `success`, `error`, `info` (default).
