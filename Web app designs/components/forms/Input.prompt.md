**Input** — glass text field with purple focus ring and optional uppercase micro-label.

```jsx
<Input label="Stream key" placeholder="sk_live_…" value={key} onChange={setKey} />
```
`onChange` receives the string value directly.
