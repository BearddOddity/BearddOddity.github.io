**FieldSection** — collapsible settings/metadata group; recessed when closed, elevated with a divider when open.

```jsx
<FieldSection title="Advanced" icon="⚙️" defaultOpen>
  <Input label="Endpoint" value={url} onChange={setUrl} />
</FieldSection>
```
