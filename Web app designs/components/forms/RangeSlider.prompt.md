**RangeSlider** — restyled range with the glowing purple thumb, optional label and live mono value.

```jsx
<RangeSlider label="Volume" value={vol} onChange={setVol} min={0} max={100} />
```
`onChange` receives a number.
